  account_id STRING COMMENT 'ID of the account where this cluster was created.',
  workspace_id STRING COMMENT 'ID of the workspace where this cluster was created.',
  cluster_id STRING COMMENT 'ID of the cluster for which this record is associated.',
  cluster_name STRING COMMENT 'User defined name for the cluster.',
  owned_by STRING COMMENT 'Username of the cluster owner. Defaults to the cluster creator, but can be changed through the [Clusters API](https://docs.databricks.com/api/workspace/clusters/changeowner).',
  create_time TIMESTAMP COMMENT 'Timestamp of the change to this compute definition.',
  delete_time TIMESTAMP COMMENT 'Timestamp of when the cluster was deleted. The value is null if the cluster is not deleted.',
  driver_node_type STRING COMMENT 'Driver node type name. This matches the instance type name from the cloud provider.',
  worker_node_type STRING COMMENT 'Worker node type name. This matches the instance type name from the cloud provider.',
  worker_count BIGINT COMMENT 'Number of workers. Defined for fixed-size clusters only.',
  min_autoscale_workers BIGINT COMMENT 'The set minimum number of workers. This field is valid only for autoscaling clusters.',
  max_autoscale_workers BIGINT COMMENT 'The set maximum number of workers. This field is valid only for autoscaling clusters.',
  auto_termination_minutes BIGINT COMMENT 'The configured autotermination duration.',
  enable_elastic_disk BOOLEAN COMMENT 'Autoscaling disk enablement status.',
  tags MAP<STRING, STRING> COMMENT 'User-defined tags for the cluster (does not include default tags).',
  cluster_source STRING COMMENT 'Where the cluster was created. For all-purpose compute, the source could be `UI` or `API`. Other compute types are logged as `JOB`, `PIPELINE`, or `PIPELINE_MAINTENANCE`.',
  init_scripts ARRAY<STRING> COMMENT 'Set of paths for init scripts.',
  aws_attributes STRUCT<first_on_demand: BIGINT, availability: STRING, zone_id: STRING, instance_profile_arn: STRING, spot_bid_price_percent: BIGINT, ebs_volume_type: STRING, ebs_volume_count: BIGINT, ebs_volume_size: BIGINT, ebs_volume_iops: BIGINT, ebs_volume_throughput: BIGINT> COMMENT 'AWS specific settings.',
  azure_attributes STRUCT<first_on_demand: BIGINT, availability: STRING, spot_bid_max_price: DOUBLE> COMMENT 'Azure specific settings.',
  gcp_attributes STRUCT<use_preemptible_executors: BOOLEAN, google_service_account: STRING, boot_disk_size: BIGINT, availability: STRING, zone_id: STRING, local_ssd_count: BIGINT> COMMENT 'GCP specific settings.',
  driver_instance_pool_id STRING COMMENT 'Instance pool ID if the driver is configured on top of an instance pool.',
  worker_instance_pool_id STRING COMMENT 'Instance Pool ID if the worker is configured on top of an instance pool.',
  dbr_version STRING COMMENT 'The Databricks Runtime of the cluster.',
  change_time TIMESTAMP COMMENT 'Timestamp of change to the compute definition.',
  change_date DATE COMMENT 'Change date. Used for retention.',
  data_security_mode STRING COMMENT 'Data security mode decides what data governance model to use when accessing data from a cluster.',
  policy_id STRING COMMENT 'The ID of the cluster policy used to create the cluster if applicable.'